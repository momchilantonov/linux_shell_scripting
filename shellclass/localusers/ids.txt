some IDs commads:
man id
id -u -> shows ID
id -un -> shows username
whoami -> shows usernamea alternatively

To assign a buildin variable to a new variable use $(id -un)
The old syntax is `id -un`

To execute an expression in if condition, you must use [[ <expression> ]]
The old syntax is with single brackets [ <expression> ]
